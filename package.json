{
  "name": "digitname",
  "version": "1.0.0",
  "description": "Digital naming and domain management tools",
  "scripts": {
    "start": "python -m http.server 8000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "canvas": "^3.1.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.0",
    "sharp": "^0.34.2"
  },
  "dependencies": {
    "mermaid": "^10.8.0"
  }
}
