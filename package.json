{
  "name": "digitname",
  "version": "1.0.0",
  "description": "Digital naming and domain management tools",
  "scripts": {
    "start": "python server.py",
    "dev": "python -m http.server 8002",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "format": "prettier --write .",
    "build": "echo 'No build step required for static site'"
  },
  "devDependencies": {
    "canvas": "^3.1.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.0",
    "sharp": "^0.34.2"
  },
  "dependencies": {
    "mermaid": "^10.8.0"
  }
}
